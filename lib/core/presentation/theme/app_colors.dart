import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF4C6FFF);
  static const secondary = Color(0xFF7C8CFF);

  // Background gradient
  static const bgTop = Color(0xFFF8F3FF);    // Tím nhạt đậm hơn (violet-50)
  static const bgBottom = Color(0xFFF0EFFF);  // Tím xanh nhạt (violet-100 → blue-50)
  // Text
  static const text = Color(0xFF374151);
  static const textStrong = Color(0xFF111827);

}
